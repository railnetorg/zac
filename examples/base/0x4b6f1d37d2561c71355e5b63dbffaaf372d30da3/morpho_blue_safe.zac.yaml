roles_modifier_address: "{{ aliases.modifiers.my_modifier }}"
safe_address: "{{ aliases.safes.my_safe }}"
chain_id: 8453
name: Base Test Safe Morpho Blue Manager
description: Supply and withdraw USDC on Morpho Blue Base, single-member test config
configs:
  - template: "../../../templates/morpho_blue/morpho_blue.tmpl"
    key: MORPHO_BLUE
    members:
      - "{{ aliases.signers.base_member }}"
    params:
      morpho_address: "{{ aliases.morpho_blue.singleton }}"
      deposit_assets:
        - address: "{{ aliases.tokens.USDC }}"
          symbol: "USDC"
      markets:
        - loan_token: "{{ aliases.morpho_blue.markets.usdc_cbeth_915.loan_token }}"
          collateral_token: "{{ aliases.morpho_blue.markets.usdc_cbeth_915.collateral_token }}"
          oracle: "{{ aliases.morpho_blue.markets.usdc_cbeth_915.oracle }}"
          irm: "{{ aliases.morpho_blue.markets.usdc_cbeth_915.irm }}"
          lltv: "{{ aliases.morpho_blue.markets.usdc_cbeth_915.lltv }}"
