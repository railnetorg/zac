# zac

```
Available recipes:
    action-audit           # audit action python dependencies
    action-format          # apply action python formatting
    action-format-check    # check action python formatting
    action-lint            # lint action python code
    action-test            # run action python tests
    action-typecheck       # type check action python code
    contracts-build        # build solidity contracts
    contracts-format       # apply solidity formatting
    contracts-format-check # check solidity formatting
    contracts-test         # run solidity tests
    install                # install foundry submodules and action dependencies
```
